/**
 * Implement a function that will find difference in 2 given arrays
 */

function difference(ar1, ar2) {
  // code
}

const assert = require("assert");
assert.deepStrictEqual(difference([1, 2, 3], [2, 3, 4]), [1, 4]);
assert.deepStrictEqual(
  difference(["an item", "another item"], ["third item"]),
  ["an item", "another item", "third item"]
);
